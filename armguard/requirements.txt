# Django and core dependencies
Django==5.1.1
python-decouple==3.8

# WSGI HTTP server for production
gunicorn==21.2.0

# Image processing and QR code generation
Pillow==10.4.0
qrcode==7.4.2

# PDF generation and form filling
reportlab==4.2.5
PyPDF2==3.0.1
PyMuPDF==1.26.5

# Security packages (production-ready)
django-ratelimit==4.1.0      # Rate limiting for views
django-axes==8.0.0           # Failed login attempt tracking and blocking
psycopg2-binary==2.9.10      # PostgreSQL adapter (for production database)

# Cache backends (production-ready)
redis==5.0.1                 # Redis client for caching (optional but recommended)
django-redis==5.4.0          # Django Redis cache backend (optional but recommended)

# Note: The following are installed system-wide on the server (not via pip):
# - Nginx (via apt install nginx) - reverse proxy for all apps
# - mkcert (manual installation from GitHub releases) - SSL certificates for LAN