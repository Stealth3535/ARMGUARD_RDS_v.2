# Django and core dependencies
Django==5.1.1
python-decouple==3.8

# WSGI HTTP server for production
gunicorn==21.2.0

# Enhanced Database Support (ARM64 Compatible)
psycopg2-binary==2.9.10      # PostgreSQL adapter (ARM64 ready)

# System monitoring (Optional - enables enhanced RPi monitoring)
# Install for production RPi deployment: pip install psutil==5.9.8
# psutil==5.9.8               # ARM64 system monitoring (optional)

# Image processing and QR code generation (ARM64 Optimized)
Pillow==10.4.0               # ARM64: Install libjpeg-dev zlib1g-dev first
qrcode[pil]==7.4.2           # ARM64 compatible with Pillow

# PDF generation and form filling (ARM64 Compatible)
reportlab==4.2.5             # ARM64 native support
PyPDF2==3.0.1               # ARM64 compatible
# PyMuPDF==1.26.5            # ARM64: May need compilation - commented out, using fallback
# ARM64 Alternative: Use reportlab for PDF operations instead of PyMuPDF

# Enhanced Security packages (ARM64 Ready)
django-ratelimit==4.1.0      # ARM64 compatible
django-axes==8.0.0           # ARM64 compatible
django-csp==3.8              # ARM64 compatible
django-security==0.12.0      # ARM64 compatible (latest available version)

# Cache backends (ARM64 Optimized)
redis==5.0.1                 # ARM64 native support
django-redis==5.4.0          # ARM64 compatible

# System monitoring (Optional for enhanced RPi features)
# Uncomment for RPi deployment: psutil==5.9.8  # ARM64 system monitoring

# ARM64-specific dependencies
wheel>=0.37.0                # Ensure proper wheel support for ARM64
setuptools>=65.0.0           # Required for ARM64 compilation

# Development dependencies (ARM64 compatible)
ipython==8.18.0              # ARM64 ready REPL

# Note: The following are installed system-wide on the server (not via pip):
# - Nginx (via apt install nginx) - reverse proxy for all apps
# - mkcert (manual installation from GitHub releases) - SSL certificates for LAN